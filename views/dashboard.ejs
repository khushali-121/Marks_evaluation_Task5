<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>dashboard</title>
    <style>
      body {
          font-family: Arial, sans-serif;
          background-color: #f4f4f4;
          margin: 0;
          padding: 0;
          display: flex;
          flex-direction: column;
          align-items: center;
          justify-content: center;
          height: 100vh;
      }

      b {
          font-size: 1.5em;
          margin-bottom: 20px;
      }

      table {
          border-collapse: collapse;
          width: 80%;
          margin-top: 20px;
      }

      th, td {
          border: 1px solid #ddd;
          padding: 10px;
          text-align: left;
      }

      th {
          background-color: #4caf50;
          color: white;
      }

      tr:nth-child(even) {
          background-color: #f2f2f2;
      }

      button {
          background-color: #4caf50;
          color: #fff;
          padding: 10px;
          margin-top: 20px;
          cursor: pointer;
          border: none;
          border-radius: 4px;
      }

      button:hover {
          background-color: #45a049;
      }
  </style>

</head>
<body>
  <b>Welcome  <%= user ? user : "Student" %> </b><br>
  <table border="2" padding="2rem">
    <tr>
        <th>Subject Name</th>
        <th>Obtained Marks</th>
        <th>Maximum Marks</th>
        <th>Result</th>
    </tr>
    
     <tr<td colspan="4"><hr>Your Percentage=> </hr><%= studentDetails.percentage %></td></tr>     
           

           <% if (studentDetails && studentDetails.marks) { %>
            <% var subjects = Object.entries(studentDetails.marks) %>
            <% subjects.forEach(item => { %>
              <tr>
                <td><%= item[0] %></td>
                <td><%= item[1] %></td>
                <td>100</td>
                <td><%= Number(item[1]) >= 55 ? "Pass" : "Fail" %></td>
              </tr>
            <% }) %>
          <% } else { %>
            <tr>
              <td colspan="4">No marks data available</td>
            </tr>
          <% } %>
          
</table>
<button  name="btn" onclick="location.href='/result'" >Get Result List</button>
</body>
</html>